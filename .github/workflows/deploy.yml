name: Test
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo "build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo "test"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy
        run: echo "deploy"
  
